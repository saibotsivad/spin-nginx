{
  "name": "demo-app",
  "version": "0.0.1",
  "description": "Just a mock module for the tests",
  "scripts": {
    "test": "echo test"
  },
  "config": {
  	"quantityOfPorts": 2,
  	"nginxConfFile": "./test.conf"
  }
}
